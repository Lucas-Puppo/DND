#!/bin/sh
SCRIPT_NAME=$(basename "$0")
APP_NAME=${SCRIPT_NAME%.sh}

DIR="${0%/*}"



"$DIR/java" $CDS_JVM_OPTS -Dfile.encoding=UTF-8 -p "$DIR/../app" -m net.rptools.tokentool/net.rptools.tokentool.client.TokenTool  "$@"
